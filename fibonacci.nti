local data is
  number : array     := [parameters];
  fib1   : procedure := call 'main' number[0] - 1
  fib2   : procedure := call 'main' number[0] - 2
end local;

main procedure is
  
  if number[0] < 2 then
    return number[0];
  else 
    return fib1 + fib2;
  end if;
  
end main;
