data section
  fib1 : int func := main()
  fib2 : int func := main()
end data;

main function ( param : int )
  
  if param < 2 then
    return param;
  else 
    return fib1(param - 1) + fib2(param - 2);
  end if;
  
end main;
